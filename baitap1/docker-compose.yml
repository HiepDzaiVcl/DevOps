# ⚠️ Bỏ dòng "version" (vì đã obsolete)
services:
  web:
    build:
      context: .        # ← dùng . vì Dockerfile nằm cùng thư mục
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host"]
